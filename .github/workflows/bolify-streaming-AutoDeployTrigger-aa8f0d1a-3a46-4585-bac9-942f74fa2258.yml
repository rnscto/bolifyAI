name: Trigger auto deployment for bolify-streaming

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/bolify-streaming-AutoDeployTrigger-aa8f0d1a-3a46-4585-bac9-942f74fa2258.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.BOLIFYSTREAMING_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.BOLIFYSTREAMING_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.BOLIFYSTREAMING_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: codeastraregistry.azurecr.io
          registryUsername: ${{ secrets.BOLIFYSTREAMING_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.BOLIFYSTREAMING_REGISTRY_PASSWORD }}
          containerAppName: bolify-streaming
          resourceGroup: vaani-ai-prod
          imageToBuild: codeastraregistry.azurecr.io/bolify-streaming:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


