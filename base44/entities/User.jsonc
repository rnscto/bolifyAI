{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user"
    },
    "client_id": {
      "type": "string",
      "description": "Associated client ID for RLS matching"
    }
  }
}